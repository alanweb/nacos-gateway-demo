spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        namespace: dev
        file-extension: yml
        group: iquantex
  profiles:
    active: dev
server:
  port: 8110
